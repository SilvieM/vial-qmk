#pragma once

// Unique Vial keyboard ID (can be regenerated later if desired)
#define VIAL_KEYBOARD_UID { 0x2B, 0xC3, 0x6F, 0x91, 0x54, 0x0E, 0xA8, 0x7D }

// Unlock combo: Esc (row 0,col 0) + Tab (row 1,col 0)
// leftmost key and the one below it on the left half
#define VIAL_UNLOCK_COMBO_ROWS { 0, 1 }
#define VIAL_UNLOCK_COMBO_COLS { 0, 0 }
